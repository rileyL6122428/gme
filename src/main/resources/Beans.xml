<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:dwr="http://www.directwebremoting.org/schema/spring-dwr"
	xmlns:mongo="http://www.springframework.org/schema/data/mongo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
					  http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
					  http://www.springframework.org/schema/context
					  http://www.springframework.org/schema/context/spring-context-3.0.xsd
					  http://www.springframework.org/schema/data/mongo
					  http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd
					  http://www.directwebremoting.org/schema/spring-dwr
					  http://www.directwebremoting.org/schema/spring-dwr-3.0.xsd">

	<context:annotation-config />
	<context:property-placeholder location="classpath*:l2k.properties" />

	<bean id="app" class="${APP}" scope="singleton" >
		<property name="level">
			<bean class="l2kstudios.gme.model.level.Level">
				<property name="playingGrid">
					<bean class="l2kstudios.gme.model.grid.PlayingGrid">
						<property name="spaces">
							<list>
								<list>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
								</list>
								<list>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
								</list>
								<list>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
								</list>
								
								<list>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" >
										<property name="occupier">
											<bean class="${UNIT}" >
												<property name="name" value="faster UNIT" />
												<property name="speed" value="3" />
												<property name="energy">
													<bean class="${CONSUMMABLE_STAT}">
														<property name="cap" value="2" />
														<property name="val" value="2" />
													</bean>
												</property>
												<property name="health">
													<bean class="${CONSUMMABLE_STAT}">
														<property name="cap" value="2" />
														<property name="val" value="2" />
													</bean>
												</property>
											</bean>
										</property>
									</bean>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" >
										<property name="occupier">
											<bean class="${UNIT}" >
												<property name="name" value="slower UNIT" />
												<property name="speed" value="2" />
												<property name="energy">
													<bean class="${CONSUMMABLE_STAT}">
														<property name="cap" value="1" />
														<property name="val" value="1" />
													</bean>
												</property>
												<property name="health">
													<bean class="${CONSUMMABLE_STAT}">
														<property name="cap" value="2" />
														<property name="val" value="2" />
													</bean>
												</property>
											</bean>
										</property>
									</bean>
								</list>
								
								<list>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
								</list>
								<list>
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
									<bean class="${SPACE}" />
								</list>
							</list>
						</property>
					</bean> 
				</property>
			</bean>		
		</property>
		
		<property name="controller" >
			<bean class="${LEVEL_CONTROLLER}" />
		</property>

		<property name="levelView" >
			<bean class="${LEVEL_VIEW}" />
		</property>		
	</bean>

	<bean id="demoConfig" class="${CONFIG}" scope="singleton" />
	
</beans>